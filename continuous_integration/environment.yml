name: mg

channels:
- conda-forge
- metagraph

dependencies:
# dev environment
  - python=3.8
  - coverage
  - pytest
  - pytest-cov
  - black=19.10b0


# dependencies (so setup.py develop doesn't pip install them)
  - metagraph::mlir
  - numpy
  - pymlir
  - llvmlite
  - donfig
  - pygments
